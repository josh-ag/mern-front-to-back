import { Typography, Box } from "@mui/material";

export const HomePage = () => {
  return (
    <Box sx={{ mt: 2 }}>
      <Typography variant="h4" sx={{ textAlign: "center", color: "#666" }}>
        Every Dream Begins With A Dreamer
      </Typography>
      <Typography variant="h2" sx={{ textAlign: "center", color: "#666" }}>
        Dream Big!
      </Typography>
    </Box>
  );
};
